import{_ as e,o as t,c as a,a as n}from"./app.4217e908.js";const i={},s=n(`<h1 id="data-migration" tabindex="-1"><a class="header-anchor" href="#data-migration" aria-hidden="true">#</a> Data Migration</h1><p>As part of the initial go-live for Pacman, we will perform a data migration to take existing data from Merlin, and import it into Pacman. From this point onwards, Pacman will be responsible for managing item &amp; pricing data, and Merlin should not be used directly.</p><h2 id="central-data-migration-instructions" tabindex="-1"><a class="header-anchor" href="#central-data-migration-instructions" aria-hidden="true">#</a> Central data migration instructions</h2><p>Note that these instructions assume a Merlin-only rollout, and do not cover Provet.</p><h3 id="data-required-from-ivc" tabindex="-1"><a class="header-anchor" href="#data-required-from-ivc" aria-hidden="true">#</a> Data required from IVC</h3><p>The data to be imported into Pacman should be supplied through various spreadsheets, which IVC have produced from Merlin exports.</p><p>See the DataMigrationsTemplates directory for the template spreadsheets.</p><p>Note that after IVC have produced this data, it&#39;s important that IVC do not make any changes to items in Merlin manually after this export has been produced, as else the items will differ between Merlin and Pacman.</p><p>The following data is required:</p><ul><li><p>Supplier Import – we need a supplier list <strong>for each supplier</strong>. See <a href="./DataMigrationTemplates/SupplierImportTemplate.csv">DataMigrationTemplates/SupplierImportTemplate.csv</a> for the expected format.</p></li><li><p>Items import – we need the items provided as per the <a href="./DataMigrationTemplates/ItemsImportTemplate.csv">DataMigrationTemplates/ItemsImportTemplate.csv</a>, which includes an example item for reference. Items must meet the Pacman validation rules, some of which are outlined below.</p><ul><li>We need an ItemType to be provided here, even though it doesn’t exist in Merlin – this is required by Pacman to determine whether an item is a stock item or service item. It doesn’t matter what the exact type is, only whether it’s a stock or service type, so feel free to hardcode in some dummy data here if it’s easier.</li><li>Boolean columns should contain Yes / No values</li><li>Null values should be represented as an empty string (as opposed to &quot;NULL&quot;, which is how these values are exported from Merlin)</li><li>Libraries should use the names in Merlin. Of particular note, this means Tax Rates should have the Merlin names, such as &quot;T1&quot; or &quot;Exempt&quot;, rather than the values (e.g. 20 or 0).</li><li>Stock Items require a Central Markup Percentage, and no Recommended Price. Service items require a Recommended Price, and no Central Markup Percentage.</li><li>Stock Items require a Units Per Pack.</li><li>Items with an HCP Group should have an HCP Multiplier.</li><li>Dispense Fee is required in Pacman (but is optional in Merlin).</li><li>Item Code max length is 25.</li><li>Item Code must not be duplicated.</li></ul></li><li><p>Items Merlin Code Ids – we need to know the internal Merlin Code IDs for each item. This is integer value for each item, that Merlin uses to uniquely identify the item. This is referred to as the &quot;Product ID&quot; in the data export provided by MWI. See the <a href="./DataMigrationTemplates/ItemMerlinCodeIdsTemplate.csv">DataMigrationTemplates/ItemMerlinCodeIdsTemplate.csv</a></p></li><li><p>Supplier products – we need to know the mapping between Supplier Products and Item Codes. See <a href="./DataMigrationTemplates/SupplierProductsTemplate.csv">DataMigrationTemplates/SupplierProductsTemplate.csv</a></p></li><li><p>Deviations – for each site that you want to test, we’ll need an export of the deviations from Merlin, as per the “Deviations import template.csv”. @Ann-Marie McElhinney, @Amy Fox – this is only required for testing the site-specific deviations – I’m not sure if this is something you’re planning to test now, or if this will be done later on? If so, we won’t need these immediately.</p></li></ul><h3 id="infrastructure-configuration" tabindex="-1"><a class="header-anchor" href="#infrastructure-configuration" aria-hidden="true">#</a> Infrastructure configuration</h3><ol><li>Ensure Merlin connection details are set in Azure Key Vault, and in the appsettings.json configuration file.</li><li>Disable sending data to Merlin. This is required to ensure that we do not recreate items in Merlin during the item data migration process. This should be done by setting the Merlin:IsSendingDataDisabled configuration value to false, either in the appsettings.json configuration file, or by adding a MERLIN__ISSENDINGDATADISABLED configuration setting on the Azure App Service configuration page.</li></ol><h3 id="initial-user-configuration" tabindex="-1"><a class="header-anchor" href="#initial-user-configuration" aria-hidden="true">#</a> Initial user configuration</h3><ol><li>Create SuperAdmin users in Pacman if required. <ol><li>This is done manually by inserting the user into the Users table in the database, and inserting a row in the UserPermissions table to grant SuperAdmin permission.</li><li>Our initial DB seed scripts currently set up John &amp; Ann-Marie from IVC, as well as Ric, Bradley, Dani and Charles from Ghyston.</li></ol></li><li>Create other users directly in Pacman <ol><li>Super admins can create other users in Pacman through the administration page, and assign permissions as required. Note that users will also need to exist in IVC&#39;s Active Directory to be able to log in.</li></ol></li></ol><h3 id="first-merlin-instance" tabindex="-1"><a class="header-anchor" href="#first-merlin-instance" aria-hidden="true">#</a> First Merlin Instance</h3><p>These steps are for integrating the first instance of Merlin into Pacman. Subsequent instances of Merlin should follow the instructions later on in this document.</p><h4 id="import-suppliers" tabindex="-1"><a class="header-anchor" href="#import-suppliers" aria-hidden="true">#</a> Import suppliers</h4><ol><li>Sync suppliers from Merlin, by clicking the &quot;Sync Suppliers&quot; button on the Suppliers page in the tool.</li><li>Upload supplier list spreadsheets <strong>for each supplier</strong>, through the &quot;Upload supplier list&quot; page in the tool.</li></ol><h4 id="import-items" tabindex="-1"><a class="header-anchor" href="#import-items" aria-hidden="true">#</a> Import items</h4><p>Items will be imported by creating them via the item bulk upload. Note that during normal operation of the system, creating items in Pacman will create the items in Merlin. During the data migration, we need to avoid recreating existing items in Merlin, which we will do by disabling data sending to Merlin.</p><ol><li>Ensure Merlin data sending is switched off in the app configuration, as per the above instructions.</li><li>Note down the Group ID of the particular instance of Merlin that is being integrated, it will be needed later</li><li>Upload items via the Item Bulk Upload page in Pacman, from the <code>ItemsImportTemplate.csv</code>. <ol><li>Pacman will perform validation on these items - you may need to fix any errors in the data.</li></ol></li><li>In the database, update Items to set the <code>MerlinCodeId</code> column with the corresponding value from Merlin, and set the <code>HasHadMerlinCreationRequestCreated</code> column to be True for all items (this ensures that Pacman does not attempt to create the items in Merlin). <ol><li>This requires taking the <code>ItemMerlinCodeIdsTemplate.csv</code> data, and mapping it to SQL update statements. This can be done by using the following formula in Excel to do the mapping, and then manually running the SQL queries: <code>=&quot;INSERT INTO ItemMerlinInstance (GroupId, ItemId, MerlinCodeId) VALUES (GROUP_ID, (SELECT Id FROM Items WHERE Code = &#39;&quot;&amp;A2&amp;&quot;&#39;), &quot;&amp;B2&amp;&quot;); UPDATE Items SET HasHadMerlinCreationRequestCreated = 1 WHERE Code =&#39;&quot;&amp;A2&amp;&quot;&#39;; &quot;&amp;IF(MOD(ROW(),500)=0, CHAR(10) &amp; &quot;GO&quot;, &quot;&quot;)</code>. This will take several minutes to complete.</li><li>As this is a manual step, we should manually validate the data is consistent after this step. Run the following query to identify items which have not been updated by this process: <code>SELECT Id, Code from Items where Id NOT IN (SELECT ItemId FROM ItemMerlinInstance WHERE GroupID = GROUP_ID);</code>. This query should not return any rows.</li></ol></li><li>Manually insert ItemSupplierProducts into the database. This should be done using the data from the <code>SupplierProductsTemplate.csv</code> file <ol><li>Note that each Item should have exactly one ItemSupplierProduct (or no supplier products at all).</li><li>Note that Stock Items will only be sent to Merlin by Pacman if the item has a supplier product.</li><li>To validate that the all rows in the spreadsheet data match a valid combination of Item, Supplier and Supplier Code from the previous data exports, do the following: <ol><li>Create a temporary table to hold data: <code>CREATE TABLE #MissingItems(SupplierName NVARCHAR(100), ItemCode NVARCHAR(100), SupplierCode NVARCHAR(100));</code></li><li>Use the following Excel formula to generate SQL queries that will insert missing rows into this table: <code>=&quot;INSERT INTO #MissingItems (SupplierName, ItemCode, &quot; &amp; &quot;SupplierCode) SELECT &#39;&quot; &amp; A2 &amp; &quot;&#39;, &#39;&quot; &amp; C2 &amp; &quot;&#39;, &#39;&quot; &amp; B2 &amp; &quot;&#39; WHERE NOT EXISTS (SELECT i.Id, sp.Id FROM Items i, Suppliers s LEFT JOIN SupplierLists SL on s.Id = SL.SupplierId LEFT JOIN SupplierProducts SP &quot; &amp; &quot; ON SL.Id = SP.SupplierListId LEFT JOIN PmsSuppliers PS on s.PmsSupplierId = PS.Id WHERE i.Code = &#39;&quot; &amp; C2 &amp; &quot;&#39;AND PS.Name = &#39;&quot; &amp; A2 &amp; &quot;&#39; AND SP.Code = &#39;&quot; &amp; B2 &amp; &quot;&#39;);&quot; &amp;IF(MOD(ROW(),500)=0, CHAR(10) &amp; &quot;GO&quot;, &quot;&quot;)</code></li><li>Run the generated queries.</li><li>Run <code>SELECT * FROM #MissingItems</code>. This should not return any records - if so, these should be investigated and corrected before proceeding.</li></ol></li><li>To map from the spreadsheet data to SQL queries, the following Excel formula can be used: <code>=&quot;INSERT INTO ItemSupplierProducts (ItemId, SupplierProductId,&quot; &amp; &quot;IsPrimary, LastModifiedByUserId) SELECT TOP 1 i.Id, sp.Id, &quot; &amp; IF(D2=&quot;YES&quot;, 1, 0) &amp; &quot;, NULL FROM Items i, Suppliers s LEFT JOIN SupplierLists SL on s.Id = SL.SupplierId LEFT JOIN SupplierProducts SP &quot; &amp; &quot; ON SL.Id = SP.SupplierListId LEFT JOIN PmsSuppliers PS on s.PmsSupplierId = PS.Id WHERE i.Code = &#39;&quot; &amp; C2 &amp; &quot;&#39;AND PS.Name = &#39;&quot; &amp; A2 &amp; &quot;&#39; AND SP.Code = &#39;&quot; &amp; B2 &amp; &quot;&#39;;&quot; &amp;IF(MOD(ROW(),500)=0, CHAR(10) &amp; &quot;GO&quot;, &quot;&quot;)</code>.</li><li>To determine which Stock items have not had any supplier products created, run the following. It&#39;s valid for items to not have any supplier products, but note that these items will not be sent to Merlin until supplier products are created.<div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> I<span class="token punctuation">.</span>Code <span class="token keyword">FROM</span> Items i
<span class="token keyword">WHERE</span> i<span class="token punctuation">.</span><span class="token keyword">Type</span> <span class="token operator">in</span> <span class="token punctuation">(</span><span class="token string">&#39;Stock&#39;</span><span class="token punctuation">)</span>
<span class="token operator">AND</span> <span class="token operator">NOT</span> <span class="token keyword">EXISTS</span> <span class="token punctuation">(</span><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> ItemSupplierProducts <span class="token keyword">WHERE</span> ItemId <span class="token operator">=</span> i<span class="token punctuation">.</span>Id<span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li>To validate that there are no items with mutliple supplier products, run the following. This should not return any rows - this will cause errors in Pacman if so, and should be manually corrected to ensure only one primary supplier product exists.<div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">select</span> I<span class="token punctuation">.</span>Code <span class="token keyword">from</span> ItemSupplierProducts
<span class="token keyword">JOIN</span> Items I <span class="token keyword">on</span> ItemSupplierProducts<span class="token punctuation">.</span>ItemId <span class="token operator">=</span> I<span class="token punctuation">.</span>Id
<span class="token keyword">Where</span> IsPrimary <span class="token operator">=</span> <span class="token number">1</span>
<span class="token keyword">GROUP</span> <span class="token keyword">BY</span> ItemId<span class="token punctuation">,</span> I<span class="token punctuation">.</span>Code
<span class="token keyword">HAVING</span> <span class="token function">COUNT</span><span class="token punctuation">(</span><span class="token operator">*</span><span class="token punctuation">)</span> <span class="token operator">&gt;</span> <span class="token number">1</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li>To validate that there are no items with a supplier product, but with no primary supplier product, run the following. this should not return any rows, and if so, should be manually corrected by assigning each item a primary supplier product.<div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">select</span> I<span class="token punctuation">.</span>Code<span class="token punctuation">,</span> <span class="token function">COUNT</span><span class="token punctuation">(</span><span class="token operator">*</span><span class="token punctuation">)</span> <span class="token keyword">from</span> Items I
<span class="token keyword">WHERE</span> <span class="token operator">NOT</span> <span class="token keyword">EXISTS</span> <span class="token punctuation">(</span><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> ItemSupplierProducts <span class="token keyword">WHERE</span> ItemId <span class="token operator">=</span> i<span class="token punctuation">.</span>Id <span class="token operator">AND</span> IsPrimary <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">)</span>
<span class="token operator">AND</span> <span class="token keyword">EXISTS</span> <span class="token punctuation">(</span><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> ItemSupplierProducts <span class="token keyword">WHERE</span> ItemId <span class="token operator">=</span> i<span class="token punctuation">.</span>Id <span class="token operator">AND</span> IsPrimary <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">)</span>
<span class="token keyword">GROUP</span> <span class="token keyword">BY</span> i<span class="token punctuation">.</span>Code<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ol></li></ol><h3 id="subsequent-merlin-instances" tabindex="-1"><a class="header-anchor" href="#subsequent-merlin-instances" aria-hidden="true">#</a> Subsequent Merlin Instances</h3><p>For subsequent Merlin instances, follow these steps:</p><ol><li>Note down the Group ID of the particular instance of Merlin that is being integrated, it will be needed later</li><li>Perform an item export from the Merlin instance, but DO NOT bulk upload it; all that is needed is to link existing items to the new instance</li><li>In the database, update Items to set the <code>MerlinCodeId</code> column with the corresponding value from Merlin, and set the <code>HasHadMerlinCreationRequestCreated</code> column to be True for all items (this ensures that Pacman does not attempt to create the items in Merlin). <ol><li>This requires taking the <code>ItemMerlinCodeIdsTemplate.csv</code> data, and mapping it to SQL update statements. This can be done by using the following formula in Excel to do the mapping, and then manually running the SQL queries: <code>=&quot;INSERT INTO ItemMerlinInstance (GroupId, ItemId, MerlinCodeId) VALUES (GROUP_ID, (SELECT Id FROM Items WHERE Code = &#39;&quot;&amp;A2&amp;&quot;&#39;), &quot;&amp;B2&amp;&quot;); UPDATE Items SET HasHadMerlinCreationRequestCreated = 1 WHERE Code =&#39;&quot;&amp;A2&amp;&quot;&#39;; &quot;&amp;IF(MOD(ROW(),500)=0, CHAR(10) &amp; &quot;GO&quot;, &quot;&quot;)</code>. This will take several minutes to complete.</li><li>As this is a manual step, we should manually validate the data is consistent after this step. Run the following query to identify items which have not been updated by this process: <code>SELECT Id, Code from Items where Id NOT IN (SELECT ItemId FROM ItemMerlinInstance WHERE GroupID = GROUP_ID);</code>. This query should not return any rows.</li></ol></li><li>If there are additional supplier products to add, it is recommended to use the main UI to import new supplier products using the template in <a href="./DataMigrationTemplates/SupplierProductsTemplate.csv">DataMigrationTemplates/SupplierProductsTemplate.csv</a></li></ol><h3 id="after-all-merlin-instances-have-been-added" tabindex="-1"><a class="header-anchor" href="#after-all-merlin-instances-have-been-added" aria-hidden="true">#</a> After all Merlin Instances Have Been Added</h3><p>There is just one step remaining:</p><ol><li>Re-enable Merlin data sending through the app configuration.</li></ol><p>At this point, items will all exist in Pacman. Items can now be updated in Pacman, and changes will be pushed through to Merlin.</p><p>Pacman can now be used for all central item configuration.</p><h2 id="site-data-migration-instructions" tabindex="-1"><a class="header-anchor" href="#site-data-migration-instructions" aria-hidden="true">#</a> Site data migration instructions</h2><p>The following steps should be followed whenever onboarding a new site into Pacman. Note that sites will first be created in draft - in this case, no data will be sent to Merlin. Once the deviations have been set up in Pacman, the site can be made live within Pacman, which will cause deviations to be sent to Merlin.</p><ol><li>If onboarding a site new to IVC, create the Site in Merlin. If onboarding an existing site, this will already exist in Merlin.</li><li>Create the Site in the sites administration page in Pacman.</li><li>The PMS should be set to Merlin, and the PMS Site Reference ID set with the correct site in Pacman. If the correct site does not show in this dropdown, ensure the site has been created in Merlin.</li><li>The site will be created in a Draft state. In this state, deviations will not be sent to Merlin.</li><li>Create site-specific deviations in Pacman as required. For existing sites, this data can be bulk uploaded through the bulk upload tool. Note this will require providing an export of deviations from Merlin for each site, in the format required by Pacman deviations spreadsheet upload. See the <a href="./DataMigrationTemplates/DeviationsImportTemplate.csv">DataMigrationTemplates/DeviationsImportTemplate.csv</a></li><li>On the edit site page in Pacman, click the &quot;Go Live&quot; button. This will update the site status, and export all deviations to Merlin.</li><li>Create users for the site</li><li>Each user will need to be first created in IVC&#39;s AD by the infrastructure team</li><li>Users can then be added to Pacman through the Pacman user administration page. A Site User Admin user can be created, who can then create users for their own site themselves.</li></ol>`,32),o=[s];function r(l,p){return t(),a("div",null,o)}const u=e(i,[["render",r],["__file","DataMigration.html.vue"]]);export{u as default};
